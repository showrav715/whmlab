# External Database Optimization Configuration for cPanel
# Add these settings to your .env file for optimal external database performance on cPanel hosting

# Cache Configuration (Using file cache for cPanel compatibility)
CACHE_STORE=file
SESSION_DRIVER=file
QUEUE_CONNECTION=database

# Database Connection Optimization
DB_CONNECTION=mysql
DB_HOST=your-external-db-host
DB_PORT=3306
DB_DATABASE=your-database
DB_USERNAME=your-username
DB_PASSWORD=your-password
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# cPanel specific database settings
# Usually your database host will be 'localhost' in cPanel
# DB_HOST=localhost
# DB_DATABASE=cpanel_username_dbname
# DB_USERNAME=cpanel_username_dbuser

# Performance Optimization Settings
# These will be handled by the optimization functions

# PHP.ini recommendations for cPanel (add to your .htaccess or php.ini):
# php_value max_execution_time 600
# php_value memory_limit 1024M
# php_value default_socket_timeout 60
# php_value mysql.connect_timeout 60
# php_value mysql.timeout 60
# php_value session.gc_maxlifetime 3600

# Database Connection Pool Settings
# If using connection pooling, configure these:
# DB_POOL_MIN=5
# DB_POOL_MAX=20
# DB_POOL_TIMEOUT=30

# Logging Configuration (for debugging)
LOG_CHANNEL=single
LOG_LEVEL=info

# Enable query logging for performance monitoring (disable in production)
DB_LOG_QUERIES=false

# Session Configuration for long operations (file-based for cPanel)
SESSION_LIFETIME=120
SESSION_EXPIRE_ON_CLOSE=false

# Queue Configuration for background processing (database-based for cPanel)
QUEUE_TABLE=jobs
QUEUE_RETRY_AFTER=90
QUEUE_MAX_TRIES=3

# cPanel specific optimizations
APP_ENV=production
APP_DEBUG=false
CACHE_PREFIX=whmlab_cache_